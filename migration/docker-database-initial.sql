create table users(
       id serial primary key,
       first_name varchar,
       last_name varchar,
       email varchar,
       mobile_number varchar,
       password varchar,
       birthday int,
       gender varchar
);
